import 'package:flutter/material.dart';

class AppColors {
  // Deep Navy Background - The "Void" or "Night"
  static const Color background = Color(0xFF0F172A);

  // Slightly lighter slate for cards/surfaces
  static const Color cardSurface = Color(0xFF1E293B);

  // Muted Gold - Represents divine light/richness without being gaudy
  static Color accent = const Color(0xFFD4AF37);

  // Soft White for primary text - easier on eyes than pure white
  static const Color textPrimary = Color(0xFFF8FAFC);

  // Slate Gray for secondary text
  static const Color textSecondary = Color(0xFF94A3B8);

  // Success/Positive (e.g., for completed prayers) - Emerald
  static const Color success = Color(0xFF10B981);

  // Error/Warning - Rose
  static const Color error = Color(0xFFF43F5E);
}
